-- TODO

CREATE DATABASE IF NOT EXISTS ${db_name} ;

CREATE USER ${db_user}@'%' IDENTIFIED BY 'somepassword';
GRANT CREATE, ALTER, INDEX, LOCK TABLES, REFERENCES, UPDATE, DELETE, DROP, SELECT, INSERT ON `${db_name}`.* TO ${db_user}@'%';
FLUSH PRIVILEGES;
